// Chat upgrade: phòng chung + tin nhắn riêng 1-1 cho Cộng Đồng Tài Xế.
import {
  addDoc, collection, doc, getDoc, limit, onSnapshot, orderBy, query,
  serverTimestamp, setDoc, where
} from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js';

const services = window.firebaseServices;
if (!services?.auth || !services?.db) throw new Error('[CommunityChat] Firebase services unavailable');
const { auth, db } = services;
const GROUP_ID = 'tai_xe_viet_nam';
let groupUnsub = null;
let privateUnsub = null;
let conversationsUnsub = null;
let activeChat = null;
let mode = 'group';
let initialized = false;

const user = () => { const u = auth.currentUser; return u && !u.isAnonymous ? u : null; };
const esc = (v='') => String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const date = ts => ts?.toDate ? new Intl.DateTimeFormat('vi-VN',{dateStyle:'short',timeStyle:'short'}).format(ts.toDate()) : 'Vừa xong';
const chatId = (a,b) => [a,b].sort().join('__');
function toast(message){const el=document.createElement('div');el.className='tc-toast';el.textContent=message;document.body.appendChild(el);setTimeout(()=>el.remove(),3200)}
async function profile(){const u=user();if(!u)return null;const ref=doc(db,'users',u.uid);const s=await getDoc(ref);const old=s.exists()?s.data():{};const p={displayName:u.displayName||old.displayName||'Tài xế',photoURL:u.photoURL||old.photoURL||'',company:old.company||'Khác'};await setDoc(ref,p,{merge:true});return p}
function login(){window.driverCommunity?.login?.()}
function shell(){return `<div class="tc-chat-switch"><button class="tc-chat-mode ${mode==='group'?'active':''}" data-chat-mode="group">👥 Phòng chung</button><button class="tc-chat-mode ${mode==='private'?'active':''}" data-chat-mode="private">🔒 Tin nhắn riêng</button></div>`}
function bindModes(){const box=document.querySelector('#tc-community .tc-chat-body');if(!box)return;box.querySelectorAll('[data-chat-mode]').forEach(b=>b.onclick=()=>{mode=b.dataset.chatMode;activeChat=null;privateUnsub?.();privateUnsub=null;renderChat()})}
function renderChat(){const view=document.querySelector('#tc-community .tc-chat-view');if(!view)return;view.querySelector('.tc-chat-header').innerHTML=`<div><h3>💬 Tin nhắn tài xế</h3><small>${mode==='group'?'Phòng chung · tất cả tài xế đã đăng nhập':'Tin nhắn 1-1 · chỉ hai tài xế nhìn thấy'}</small></div><span class="tc-online">● Realtime</span>`;if(mode==='group')renderGroup();else if(activeChat)renderPrivate();else renderPrivateList()}
function renderGroup(){const body=document.querySelector('#tc-community .tc-chat-body');if(!body)return;const u=user();groupUnsub?.();if(!u){body.innerHTML=`${shell()}<div class="tc-empty"><div>🔐</div><strong>Đăng nhập để vào phòng chung</strong><p>Phòng chung dành cho anh em tài xế đã xác thực Google.</p><button class="tc-empty-btn" data-login>Đăng nhập Google</button></div>`;body.querySelector('[data-login]').onclick=login;bindModes();return}
 body.innerHTML=`${shell()}<div class="tc-group-banner"><div class="tc-group-icon">👥</div><div><strong>Phòng chung Tổ Nghề Taxi Việt Nam</strong><span>Tất cả tài xế đã đăng nhập đều có thể tham gia</span></div></div><div class="tc-group-messages"><div class="tc-empty small">Đang kết nối phòng chung…</div></div><form class="tc-group-form"><input name="text" maxlength="1000" placeholder="Nhắn cho anh em tài xế…" autocomplete="off" required><button>➤</button></form>`;body.querySelector('form').onsubmit=sendGroup;bindModes();
 groupUnsub=onSnapshot(query(collection(db,'community_rooms',GROUP_ID,'messages'),orderBy('createdAt','asc'),limit(300)),snap=>{const box=body.querySelector('.tc-group-messages');if(!box)return;if(snap.empty){box.innerHTML='<div class="tc-empty small"><div>👥</div><strong>Phòng đang chờ tin nhắn đầu tiên</strong><p>Hãy chào anh em tài xế.</p></div>';return}box.innerHTML=snap.docs.map(d=>{const m=d.data(),mine=m.senderId===u.uid;return `<div class="tc-group-msg ${mine?'mine':''}"><div class="tc-avatar mini">${m.senderPhoto?`<img src="${esc(m.senderPhoto)}" alt="">`:'👤'}</div><div class="tc-group-msg-main"><strong>${esc(m.senderName||'Tài xế')}</strong><div class="tc-group-bubble">${esc(m.content||'').replaceAll('\\n','<br>')}</div><small>${date(m.createdAt)}</small></div></div>`}).join('');box.scrollTop=box.scrollHeight},()=>{const box=body.querySelector('.tc-group-messages');if(box)box.innerHTML='<div class="tc-empty small"><div>⚠️</div><strong>Không thể kết nối phòng chung</strong><p>Kiểm tra Firestore Rules và mạng.</p></div>'})}
async function sendGroup(e){e.preventDefault();const u=user();if(!u)return login();const f=e.currentTarget,t=f.text.value.trim();if(!t)return;const b=f.querySelector('button');b.disabled=true;try{const p=await profile();await addDoc(collection(db,'community_rooms',GROUP_ID,'messages'),{senderId:u.uid,senderName:p?.displayName||u.displayName||'Tài xế',senderPhoto:p?.photoURL||u.photoURL||'',content:t.slice(0,1000),createdAt:serverTimestamp()});f.reset()}catch(e){console.error(e);toast('Không thể gửi tin nhắn phòng chung.')}finally{b.disabled=false}}
function renderPrivateList(){const body=document.querySelector('#tc-community .tc-chat-body');if(!body)return;const u=user();conversationsUnsub?.();if(!u){body.innerHTML=`${shell()}<div class="tc-empty"><div>🔐</div><strong>Đăng nhập để nhắn tin riêng</strong><p>Tin nhắn riêng chỉ hiển thị với đúng 2 tài xế.</p><button class="tc-empty-btn" data-login>Đăng nhập Google</button></div>`;body.querySelector('[data-login]').onclick=login;bindModes();return}
 body.innerHTML=`${shell()}<div class="tc-private-intro"><strong>🔒 Tin nhắn riêng tư</strong><span>Chỉ bạn và người nhận mới đọc được tin nhắn.</span></div><div class="tc-chat-list-title">Cuộc trò chuyện của tôi</div><div class="tc-conversation-list"><div class="tc-empty small">Đang tải…</div></div><div class="tc-private-help">💡 Muốn nhắn cho một tài xế: vào <b>Bản tin</b> → chọn bài viết → <b>Nhắn tin</b>.</div>`;bindModes();
 conversationsUnsub=onSnapshot(query(collection(db,'direct_chats'),where('participantIds','array-contains',u.uid),limit(50)),snap=>{const box=body.querySelector('.tc-conversation-list');if(!box)return;const rows=snap.docs.map(d=>({id:d.id,d:d.data()})).sort((a,b)=>(b.d.lastMessageAt?.toMillis?.()||0)-(a.d.lastMessageAt?.toMillis?.()||0));if(!rows.length){box.innerHTML='<div class="tc-empty small"><div>💬</div><strong>Chưa có cuộc trò chuyện</strong><p>Mở một bài viết và bấm Nhắn tin để bắt đầu.</p></div>';return}box.innerHTML=rows.map(x=>{const other=(x.d.participantIds||[]).find(id=>id!==u.uid);const p=x.d.participantProfiles?.[other]||{};return `<button class="tc-conversation" data-open-private="${esc(other)}" data-name="${esc(p.name||'Tài xế')}" data-photo="${esc(p.photo||'')}" data-chat="${esc(x.id)}"><div class="tc-avatar">${p.photo?`<img src="${esc(p.photo)}" alt="">`:'👤'}</div><div><strong>${esc(p.name||'Tài xế')}</strong><small>${esc(x.d.lastMessage||'Bắt đầu cuộc trò chuyện')}</small></div><span>${date(x.d.lastMessageAt)}</span></button>`}).join('');box.querySelectorAll('[data-open-private]').forEach(b=>b.onclick=()=>openPrivate({uid:b.dataset.openPrivate,name:b.dataset.name,photo:b.dataset.photo},b.dataset.chat))})
}
async function openPrivate(p,id=null){const u=user();if(!u)return login();if(!p?.uid||p.uid===u.uid)return;mode='private';activeChat={...p,id:id||chatId(u.uid,p.uid)};privateUnsub?.();privateUnsub=null;renderChat();const ref=doc(db,'direct_chats',activeChat.id);try{const me=await profile();await setDoc(ref,{participantIds:[u.uid,p.uid].sort(),participantProfiles:{[u.uid]:{name:me?.displayName||u.displayName||'Tài xế',photo:me?.photoURL||u.photoURL||''},[p.uid]:{name:p.name||'Tài xế',photo:p.photo||''}}},{merge:true});subscribePrivate()}catch(e){console.error(e);toast('Không thể mở tin nhắn riêng.')}}
function renderPrivate(){const body=document.querySelector('#tc-community .tc-chat-body');if(!body||!activeChat)return;body.innerHTML=`${shell()}<div class="tc-private-header"><button class="tc-chat-back">←</button><div class="tc-avatar">${activeChat.photo?`<img src="${esc(activeChat.photo)}" alt="">`:'👤'}</div><div><strong>${esc(activeChat.name||'Tài xế')}</strong><small>🔒 Chỉ bạn và người nhận nhìn thấy</small></div></div><div class="tc-private-messages"><div class="tc-empty small">Đang mở cuộc trò chuyện…</div></div><form class="tc-private-form"><input name="text" maxlength="1000" placeholder="Nhắn tin riêng…" autocomplete="off" required><button>➤</button></form>`;body.querySelector('.tc-chat-back').onclick=()=>{privateUnsub?.();privateUnsub=null;activeChat=null;renderPrivateList()};body.querySelector('form').onsubmit=sendPrivate;bindModes();subscribePrivate()}
function subscribePrivate(){privateUnsub?.();const u=user();if(!u||!activeChat)return;privateUnsub=onSnapshot(query(collection(db,'direct_chats',activeChat.id,'messages'),orderBy('createdAt','asc'),limit(300)),snap=>{const box=document.querySelector('#tc-community .tc-private-messages');if(!box)return;if(snap.empty){box.innerHTML=`<div class="tc-empty small"><div>💬</div><strong>Bắt đầu cuộc trò chuyện</strong><p>Hãy gửi lời chào tới ${esc(activeChat.name||'tài xế')}.</p></div>`;return}box.innerHTML=snap.docs.map(d=>{const m=d.data(),mine=m.senderId===u.uid;return `<div class="tc-dm-msg ${mine?'mine':''}"><div class="tc-avatar mini">${m.senderPhoto?`<img src="${esc(m.senderPhoto)}" alt="">`:'👤'}</div><div><div class="tc-dm-bubble">${esc(m.content||'').replaceAll('\\n','<br>')}</div><small>${date(m.createdAt)}</small></div></div>`}).join('');box.scrollTop=box.scrollHeight},()=>{const box=document.querySelector('#tc-community .tc-private-messages');if(box)box.innerHTML='<div class="tc-empty small">Không thể kết nối tin nhắn riêng.</div>'})}
async function sendPrivate(e){e.preventDefault();const u=user();if(!u||!activeChat)return login();const f=e.currentTarget,t=f.text.value.trim();if(!t)return;const b=f.querySelector('button');b.disabled=true;try{const p=await profile();await setDoc(doc(db,'direct_chats',activeChat.id),{lastMessage:t.slice(0,160),lastMessageAt:serverTimestamp()},{merge:true});await addDoc(collection(db,'direct_chats',activeChat.id,'messages'),{senderId:u.uid,senderName:p?.displayName||u.displayName||'Tài xế',senderPhoto:p?.photoURL||u.photoURL||'',content:t.slice(0,1000),createdAt:serverTimestamp()});f.reset()}catch(e){console.error(e);toast('Không thể gửi tin nhắn riêng.')}finally{b.disabled=false}}
function install(){if(initialized)return;initialized=true;const old=window.driverCommunity?.open;if(!old)return;window.driverCommunity.open=()=>{old();setTimeout(()=>patchPanel(),0)};document.addEventListener('click',e=>{const b=e.target.closest?.('[data-message-user]');if(!b||!document.querySelector('#tc-community'))return;e.preventDefault();e.stopImmediatePropagation();openPrivate({uid:b.dataset.messageUser,name:b.dataset.messageName,photo:b.dataset.messagePhoto})},true);window.addEventListener('firebase-auth-changed',()=>{if(document.querySelector('#tc-community')){patchPanel();}})}
function patchPanel(){const view=document.querySelector('#tc-community .tc-chat-view');if(!view)return;view.querySelector('.tc-chat-header').innerHTML=`<div><h3>💬 Tin nhắn tài xế</h3><small>Phòng chung · Tin nhắn riêng 1-1</small></div><span class="tc-online">● Realtime</span>`;renderChat()}
install();
